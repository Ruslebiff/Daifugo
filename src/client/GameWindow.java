package client;

import javax.swing.*;

public class GameWindow extends JFrame {
    public GameWindow() {
        setSize(1000,1000);
        setLayout(null);
        setTitle("Daifugo");
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        setVisible(true);
    }
}
